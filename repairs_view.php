<?php
// This script and data application were generated by AppGini 5.51
// Download AppGini for free from http://bigprof.com/appgini/download/

	$currDir=dirname(__FILE__);
	include("$currDir/defaultLang.php");
	include("$currDir/language.php");
	include("$currDir/lib.php");
	@include("$currDir/hooks/repairs.php");
	include("$currDir/repairs_dml.php");

	// mm: can the current member access this page?
	$perm=getTablePermissions('repairs');
	if(!$perm[0]){
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout("window.location=\'index.php?signOut=1\'", 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = "repairs";

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV=array(   
		"`repairs`.`id`" => "id",
		"`repairs`.`repair_status`" => "repair_status",
		"IF(    CHAR_LENGTH(`inventory1`.`id`) || CHAR_LENGTH(`inventory1`.`item`), CONCAT_WS('',   `inventory1`.`id`, ' - ', `inventory1`.`item`), '') /* Name */" => "item",
		"`repairs`.`perform`" => "perform",
		"`repairs`.`repair_notes`" => "repair_notes",
		"CONCAT('$', FORMAT(`repairs`.`cost`, 2))" => "cost",
		"`repairs`.`batch_number`" => "batch_number",
		"`repairs`.`photo_1`" => "photo_1",
		"`repairs`.`photo_2`" => "photo_2",
		"`repairs`.`last_update`" => "last_update"
	);
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = array(   
		1 => '`repairs`.`id`',
		2 => 2,
		3 => 3,
		4 => 4,
		5 => 5,
		6 => '`repairs`.`cost`',
		7 => 7,
		8 => 8,
		9 => 9,
		10 => 10
	);

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV=array(   
		"`repairs`.`id`" => "id",
		"`repairs`.`repair_status`" => "repair_status",
		"IF(    CHAR_LENGTH(`inventory1`.`id`) || CHAR_LENGTH(`inventory1`.`item`), CONCAT_WS('',   `inventory1`.`id`, ' - ', `inventory1`.`item`), '') /* Name */" => "item",
		"`repairs`.`perform`" => "perform",
		"`repairs`.`repair_notes`" => "repair_notes",
		"CONCAT('$', FORMAT(`repairs`.`cost`, 2))" => "cost",
		"`repairs`.`batch_number`" => "batch_number",
		"`repairs`.`photo_1`" => "photo_1",
		"`repairs`.`photo_2`" => "photo_2",
		"`repairs`.`last_update`" => "last_update"
	);
	// Fields that can be filtered
	$x->QueryFieldsFilters=array(   
		"`repairs`.`id`" => "ID",
		"`repairs`.`repair_status`" => "Repair status",
		"IF(    CHAR_LENGTH(`inventory1`.`id`) || CHAR_LENGTH(`inventory1`.`item`), CONCAT_WS('',   `inventory1`.`id`, ' - ', `inventory1`.`item`), '') /* Name */" => "Name",
		"`repairs`.`perform`" => "Perform",
		"`repairs`.`repair_notes`" => "Repair notes",
		"`repairs`.`cost`" => "Cost",
		"`repairs`.`batch_number`" => "Batch ID"
	);

	// Fields that can be quick searched
	$x->QueryFieldsQS=array(   
		"`repairs`.`id`" => "id",
		"`repairs`.`repair_status`" => "repair_status",
		"IF(    CHAR_LENGTH(`inventory1`.`id`) || CHAR_LENGTH(`inventory1`.`item`), CONCAT_WS('',   `inventory1`.`id`, ' - ', `inventory1`.`item`), '') /* Name */" => "item",
		"`repairs`.`perform`" => "perform",
		"`repairs`.`repair_notes`" => "repair_notes",
		"CONCAT('$', FORMAT(`repairs`.`cost`, 2))" => "cost",
		"`repairs`.`batch_number`" => "batch_number"
	);

	// Lookup fields that can be used as filterers
	$x->filterers = array(  'item' => 'Name');

	$x->QueryFrom="`repairs` LEFT JOIN `inventory` as inventory1 ON `inventory1`.`id`=`repairs`.`item` ";
	$x->QueryWhere='';
	$x->QueryOrder='';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm[2]==0 ? 1 : 0);
	$x->AllowDelete = $perm[4];
	$x->AllowMassDelete = false;
	$x->AllowInsert = $perm[1];
	$x->AllowUpdate = $perm[3];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = 1;
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation["quick search"];
	$x->ScriptFileName = "repairs_view.php";
	$x->RedirectAfterInsert = "repairs_view.php?SelectedID=#ID#";
	$x->TableTitle = "Repairs";
	$x->TableIcon = "resources/table_icons/repair.png";
	$x->PrimaryKey = "`repairs`.`id`";
	$x->DefaultSortField = '7';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth   = array(  150, 150, 150, 150, 80, 40, 150, 150);
	$x->ColCaption = array("Repair status", "Name", "Perform", "Repair notes", "Cost", "Batch ID", "Photo 1", "Photo 2");
	$x->ColFieldName = array('repair_status', 'item', 'perform', 'repair_notes', 'cost', 'batch_number', 'photo_1', 'photo_2');
	$x->ColNumber  = array(2, 3, 4, 5, 6, 7, 8, 9);

	$x->Template = 'templates/repairs_templateTV.html';
	$x->SelectedTemplate = 'templates/repairs_templateTVS.html';
	$x->ShowTableHeader = 1;
	$x->ShowRecordSlots = 0;
	$x->HighlightColor = '#FFF0C2';

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, array('user', 'group'))){ $DisplayRecords = 'all'; }
	if($perm[2]==1 || ($perm[2]>1 && $DisplayRecords=='user' && !$_REQUEST['NoFilter_x'])){ // view owner only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `repairs`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='repairs' and lcase(membership_userrecords.memberID)='".getLoggedMemberID()."'";
	}elseif($perm[2]==2 || ($perm[2]>2 && $DisplayRecords=='group' && !$_REQUEST['NoFilter_x'])){ // view group only
		$x->QueryFrom.=', membership_userrecords';
		$x->QueryWhere="where `repairs`.`id`=membership_userrecords.pkValue and membership_userrecords.tableName='repairs' and membership_userrecords.groupID='".getLoggedGroupID()."'";
	}elseif($perm[2]==3){ // view all
		// no further action
	}elseif($perm[2]==0){ // view none
		$x->QueryFields = array("Not enough permissions" => "NEP");
		$x->QueryFrom = '`repairs`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: repairs_init
	$render=TRUE;
	if(function_exists('repairs_init')){
		$args=array();
		$render=repairs_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// column sums
	if(strpos($x->HTML, '<!-- tv data below -->')){
		// if printing multi-selection TV, calculate the sum only for the selected records
		if(isset($_REQUEST['Print_x']) && is_array($_REQUEST['record_selector'])){
			$QueryWhere = '';
			foreach($_REQUEST['record_selector'] as $id){   // get selected records
				if($id != '') $QueryWhere .= "'" . makeSafe($id) . "',";
			}
			if($QueryWhere != ''){
				$QueryWhere = 'where `repairs`.`id` in ('.substr($QueryWhere, 0, -1).')';
			}else{ // if no selected records, write the where clause to return an empty result
				$QueryWhere = 'where 1=0';
			}
		}else{
			$QueryWhere = $x->QueryWhere;
		}

		$sumQuery="select CONCAT('$', FORMAT(sum(`repairs`.`cost`), 2)) from ".$x->QueryFrom.' '.$QueryWhere;
		$res=sql($sumQuery, $eo);
		if($row=db_fetch_row($res)){
			$sumRow ="<tr class=\"success\">";
			if(!isset($_REQUEST['Print_x'])) $sumRow.="<td class=\"text-center\"><strong>&sum;</strong></td>";
			$sumRow.="<td></td>";
			$sumRow.="<td></td>";
			$sumRow.="<td></td>";
			$sumRow.="<td></td>";
			$sumRow.="<td class=\"text-right\">{$row[0]}</td>";
			$sumRow.="<td></td>";
			$sumRow.="<td></td>";
			$sumRow.="<td></td>";
			$sumRow.="</tr>";

			$x->HTML=str_replace("<!-- tv data below -->", '', $x->HTML);
			$x->HTML=str_replace("<!-- tv data above -->", $sumRow, $x->HTML);
		}
	}

	// hook: repairs_header
	$headerCode='';
	if(function_exists('repairs_header')){
		$args=array();
		$headerCode=repairs_header($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$headerCode){
		include_once("$currDir/header.php"); 
	}else{
		ob_start(); include_once("$currDir/header.php"); $dHeader=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%HEADER%%>', $dHeader, $headerCode);
	}

	echo $x->HTML;
	// hook: repairs_footer
	$footerCode='';
	if(function_exists('repairs_footer')){
		$args=array();
		$footerCode=repairs_footer($x->ContentType, getMemberInfo(), $args);
	}  
	if(!$footerCode){
		include_once("$currDir/footer.php"); 
	}else{
		ob_start(); include_once("$currDir/footer.php"); $dFooter=ob_get_contents(); ob_end_clean();
		echo str_replace('<%%FOOTER%%>', $dFooter, $footerCode);
	}
?>